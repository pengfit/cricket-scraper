version: "3.8"

services:
  gov_xian:
    build: .
    environment:
      - GOV_MODULE = gov_xian
      - MODULE_URL = https://zjj.xa.gov.cn/zxcx/gczj/indexHis.aspx?page=1&qdm=ZZXU
      - REPORTS_DIR = app/src/
    ipc: host
    volumes:
      - .:/app

networks:
  elastic:
    external: true